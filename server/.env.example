# Puerto del servidor
PORT=4000

# Origen permitido por CORS (dominio del frontend).
# '*' permite todos (útil para desarrollo). En producción usa tu dominio de Vercel.
# Ejemplo: https://tu-app.vercel.app
CLIENT_ORIGIN=*